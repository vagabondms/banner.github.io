import { css } from '@emotion/react';

const mainSize = css`
  font-size: 13px;
`;

const largeSize = css`
  font-size: 15px;
`;

const buttonSize = css`
  font-size: 18px;
`;

export default {
  mainSize,
  largeSize,
  buttonSize,
};
